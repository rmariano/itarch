<?xml version="1.0" encoding="utf-8"?>
<rss version="2.0" xmlns:dc="http://purl.org/dc/elements/1.1/"><channel><title>IT Arch (development)</title><link>http://rmariano.github.io/itarch</link><description></description><language>es</language><lastBuildDate>Sat, 28 Jun 2014 17:20:37 GMT</lastBuildDate><generator>http://getnikola.com/</generator><docs>http://blogs.law.harvard.edu/tech/rss</docs><item><title>Default arguments in Python functions</title><link>http://rmariano.github.io/es/posts/default-arguments-in-python-functions.html</link><dc:creator>Mariano Anaya</dc:creator><description>&lt;p&gt;This post is based on a gist I wrote a while ago, about why is
not a good idea to pass default mutable objects as parameters
in python function definitions.&lt;/p&gt;
&lt;p&gt;While the gist is explained through an example that uses lists, the
principle is applicable to all sorts of objects (dictionaries, sets, etc.).&lt;/p&gt;
&lt;script src="https://gist.github.com/7593536.js"&gt;&lt;/script&gt;&lt;noscript&gt;&lt;pre class="literal-block"&gt;
[![License](http://img.shields.io/license/MIT.png?color=green)](http://opensource.org/licenses/MIT)
#\#Python - How to pass empty lists as default argument


On this gist I would like to point out an aspect of Python that might present
some issues if it is not implemented correctly.

Suppose we have a function and we need to define that some of its parameters
take an empty list as a default argument. Something like the following, for
example, might be a typical implementation:

```python

    def process_list(a, b=[]):
        b.extend(a)
        print(b)
```

Here the first line with `b=[]` presents a problem.
Under this definition, if we run the following:

```python
    process_list([1,2,3])
    process_list([4,5], [0])
    process_list([7,8,9])
```

This is what you get, compared to what you would expect.



Expected | Obtained
---------|---------------
[1, 2, 3]| [1, 2, 3]
[0, 4, 5]| [0, 4, 5]
[7, 8, 9]| [1,2,3,7,8,9]


The problem is that the second time that you try to use `b` (on the third
function call), Python has already defined it, so it will keep the value that
had the first time it was declared (when it parsed the function definition).

To avoid this, you should give the parameter a different value (usually called
_sentinel_, and assign the default value manually, for example:

```python
    def process_list(a, b=None):
        b = b or []
        b.extend(a)
        print(b)
```

This time, it will work properly.

To sum up, please bear in mind this when defining a function of this kind
in Python, and also take into consideration that the same applies not only for
lists but for sets, or dictionaries as well.

&lt;/pre&gt;
&lt;/noscript&gt;&lt;p&gt;If you are an experienced python developer, you probably knew this caveat,
nevertheless is something interesting to show to new python developers, and
to remember even if you have been writing code in Python for years.&lt;/p&gt;</description><guid>http://rmariano.github.io/es/posts/default-arguments-in-python-functions.html</guid><pubDate>Sat, 28 Jun 2014 19:59:41 GMT</pubDate></item><item><title>On returning consistent data types</title><link>http://rmariano.github.io/es/posts/python-functions-return-values-consistency.html</link><dc:creator>Mariano Anaya</dc:creator><description>&lt;p&gt;This post is inspired on an issue I once found, while I was using a well-known library in Python for
parsing YAML files. The problem was that when it was loading the content of the file, the result was
not coherent, because sometimes it returned the content as a python &lt;strong&gt;dict&lt;/strong&gt;, but if the file was empty, the
return value was &lt;strong&gt;None&lt;/strong&gt;.&lt;/p&gt;
&lt;p&gt;Do you notice something odd here?&lt;/p&gt;
&lt;p&gt;What if I want to use the result? I cannot do it safely, for example:&lt;/p&gt;
&lt;pre class="code python literal-block"&gt;
&lt;span class="ln"&gt;1 &lt;/span&gt;&lt;span class="n"&gt;content&lt;/span&gt; &lt;span class="o"&gt;=&lt;/span&gt; &lt;span class="n"&gt;yaml&lt;/span&gt;&lt;span class="o"&gt;.&lt;/span&gt;&lt;span class="n"&gt;load&lt;/span&gt;&lt;span class="p"&gt;(&lt;/span&gt;&lt;span class="o"&gt;...&lt;/span&gt;&lt;span class="p"&gt;)&lt;/span&gt;  &lt;span class="c"&gt;# with the correct parameters and file name&lt;/span&gt;
&lt;span class="ln"&gt;2 &lt;/span&gt;&lt;span class="k"&gt;for&lt;/span&gt; &lt;span class="n"&gt;tag&lt;/span&gt;&lt;span class="p"&gt;,&lt;/span&gt; &lt;span class="n"&gt;values&lt;/span&gt; &lt;span class="ow"&gt;in&lt;/span&gt; &lt;span class="n"&gt;content&lt;/span&gt;&lt;span class="o"&gt;.&lt;/span&gt;&lt;span class="n"&gt;items&lt;/span&gt;&lt;span class="p"&gt;():&lt;/span&gt;
&lt;span class="ln"&gt;3 &lt;/span&gt;    &lt;span class="k"&gt;pass&lt;/span&gt;  &lt;span class="c"&gt;# process as required...&lt;/span&gt;
&lt;/pre&gt;
&lt;p&gt;If content is &lt;tt class="docutils literal"&gt;None&lt;/tt&gt;, it will raise an &lt;tt class="docutils literal"&gt;AttributeError&lt;/tt&gt; saying that None has no
attribute called "items" (which is true).&lt;/p&gt;
&lt;p&gt;Therefore, the developer should catch the exception or avoid the corner case,
by doing something like the following:&lt;/p&gt;
&lt;pre class="code python literal-block"&gt;
&lt;span class="n"&gt;content&lt;/span&gt; &lt;span class="o"&gt;=&lt;/span&gt; &lt;span class="n"&gt;yaml&lt;/span&gt;&lt;span class="o"&gt;.&lt;/span&gt;&lt;span class="n"&gt;load&lt;/span&gt;&lt;span class="p"&gt;()&lt;/span&gt; &lt;span class="ow"&gt;or&lt;/span&gt; &lt;span class="p"&gt;{}&lt;/span&gt;
&lt;/pre&gt;
&lt;p&gt;That could be a case of "coding defensively", making sure that the program will not fail under
most conditions (it would also require to add an &lt;tt class="docutils literal"&gt;assert&lt;/tt&gt; or to raise an exception
perhaps, but that is a different topic). I actually agree with defensive programming,
but I think it is better if the library itself has a more correct behaviour, respecting
the interface (that is: if you are going to return a dictionary, and there is not content,
then the logical assumption is to expect an empty dictionary).
This must be the default behaviour, not something to be set by parameters.&lt;/p&gt;
&lt;p&gt;This could be thought as an instance of a more general problem that occurs when some function
is intended to return "X or Y". In my opinion, if X and Y do not share the same interface,
there is a potential bug (in the Object-Oriented paradigm we would say that there
is no polymorphism, or maybe that the "contract" is not being respected).&lt;/p&gt;
&lt;p&gt;This is an example that I wanted to highlight, because it might help you to write cleaner code.&lt;/p&gt;</description><guid>http://rmariano.github.io/es/posts/python-functions-return-values-consistency.html</guid><pubDate>Sun, 08 Jun 2014 18:46:09 GMT</pubDate></item></channel></rss>