CREATE TABLE orders (
    id integer,
    description VARCHAR(500),
    order_date timestamp
);


CREATE TABLE archive_orders (
    id integer,
    description VARCHAR(500),
    order_date timestamp
);
