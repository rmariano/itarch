<?xml version="1.0" encoding="utf-8"?>
<rss version="2.0" xmlns:dc="http://purl.org/dc/elements/1.1/"><channel><title>IT Arch (rabbitmq)</title><link>http://rmariano.github.io/itarch/</link><description></description><language>en</language><lastBuildDate>Sun, 03 May 2015 19:10:48 GMT</lastBuildDate><generator>http://getnikola.com/</generator><docs>http://blogs.law.harvard.edu/tech/rss</docs><item><title>Running RabbitMQ server on Docker</title><link>http://rmariano.github.io/itarch/posts/running-rabbitmq-server-on-docker.html</link><dc:creator>Mariano Anaya</dc:creator><description>&lt;p&gt;If you use RabbitMQ for development frequently, sometimes you might have found
it uses too much resources (it's normal while programming to have
a lot of queues or tasks being queued and that makes the CPU usage to spike).&lt;/p&gt;
&lt;p&gt;Having RabbitMQ installed on the OS seems the perfect approach for production, but
on development I'd rather do something different, in order to isolate
the process. I know I could bound it (for example order it not to start automatically
as a dæmon), by means of &lt;code&gt;systemd&lt;/code&gt; but a few weeks ago I decided to
try &lt;code&gt;docker&lt;/code&gt; and see how it results.&lt;/p&gt;
&lt;p&gt;It turned out to be just the tool for the work, and so far with a little simple
configuration it can run as expected.&lt;/p&gt;
&lt;p&gt;There is already a docker image for RabbitMQ, which can be automatically pulled,
and then run, for example:&lt;/p&gt;
&lt;pre class="code bash literal-block"&gt;
sudo docker pull rabbitmq
sudo docker run -d -e &lt;span class="nv"&gt;RABBITMQ_NODENAME&lt;/span&gt;&lt;span class="o"&gt;=&lt;/span&gt;my-rabbit --cpuset&lt;span class="o"&gt;=&lt;/span&gt;&lt;span class="s2"&gt;"1"&lt;/span&gt; --name docker.rabbitmq -p 5672:5672 rabbitmq:3
&lt;/pre&gt;
&lt;p&gt;The &lt;code&gt;-d&lt;/code&gt; option indicates the process to start &lt;em&gt;detached&lt;/em&gt;, then by
passing &lt;code&gt;-e&lt;/code&gt; we pass some environment variables (in this case,
the &lt;code&gt;RABBITMQ_NODENAME&lt;/code&gt; is a particular variable for rabbit indicating
how to set the name of the node it is starting). Optionally, we can limit the CPU
usage with the &lt;code&gt;--cpuset&lt;/code&gt;, as in this case which sets the process to use the
second core of the machine (it starts at &lt;cite&gt;0&lt;/cite&gt;). Then the &lt;code&gt;--name&lt;/code&gt; is a name for the docker being created.&lt;/p&gt;
&lt;p&gt;The most important part in this case is the port mapping, made by the &lt;code&gt;-p&lt;/code&gt; option which in this case maps the
port used by RabbitMQ directly (1:1) with the host machine. This makes the docker process to run transparently, as
the other applications that try to communicate with a RabbitMQ won't notice any difference, making it look like is
executing an actual RabbitMQ service. Finally there is the name of the docker image to run.&lt;/p&gt;
&lt;p&gt;What I usually do is to manage the docker image by its &lt;cite&gt;instance_id&lt;/cite&gt; (a number that is displayed after listing
the docker images, by doing &lt;code&gt;sudo docker ps -a&lt;/code&gt;).
Then we can manage it by &lt;code&gt;sudo docker [start|stop] &amp;lt;instance_id&amp;gt;&lt;/code&gt;.&lt;/p&gt;
&lt;p&gt;There is another command to see the output being generated by the process
which is &lt;code&gt;docker logs rabbitmq.docker&lt;/code&gt;. Notice in this case the name
designated to the image was used instead of the &lt;em&gt;instance_id&lt;/em&gt;. In addition
we can see internal data for the process by running the &lt;code&gt;inspect&lt;/code&gt;
command (again we can use the &lt;cite&gt;instance_id&lt;/cite&gt; or the name we assigned).&lt;/p&gt;
&lt;pre class="code bash literal-block"&gt;
docker inspect rabbitmq.docker
sudo docker logs docker.rabbitmq
&lt;/pre&gt;
&lt;p&gt;It's important to notice that docker is actually &lt;em&gt;not a virtualization platform&lt;/em&gt;, but a mechanism that runs processes
in containers, meaning that in this case the entire RabbitMQ is running as a single process within a container, with some
other limitations and bounds constrained by &lt;code&gt;docker&lt;/code&gt;.&lt;/p&gt;
&lt;p&gt;I found this approach to be very versatile for a development environment, and with RabbitMQ being the first pilot,
I think I can migrate more applications to docker instead of having them installed on the system (as long as possible).&lt;/p&gt;</description><category>docker</category><category>linux</category><category>rabbitmq</category><guid>http://rmariano.github.io/itarch/posts/running-rabbitmq-server-on-docker.html</guid><pubDate>Sun, 26 Apr 2015 16:58:46 GMT</pubDate></item></channel></rss>